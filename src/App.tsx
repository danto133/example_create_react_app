import React from "react";
import "./App.scss";

const App: React.FC = () => (
  <div className="App">
    App
  </div>
);

const AppWrapper: React.FC = () => (
  // TODO: Update Provider later
  <App />
);

export default AppWrapper;
